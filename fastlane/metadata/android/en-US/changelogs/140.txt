• Fixed a rare case where the automatic recovery attempt could leave Wireless Debugging stuck off instead of turning it back on, and made sure the app's display always reflects the real result.
• Fixed a crash risk on devices that restrict reading the Wireless Debugging switch even while KeepADB still holds the write permission; KeepADB now falls back safely instead of crashing.
