Keep the display on while KeepADB is in the foreground (optional, off by default). The security advice banner on the main screen is readable again — dark surface, drawn warning icon, no more white text on yellow — and now carries the complete security advice in all 19 supported languages, so the separate "Security & Network Advice" section in Settings is gone. The banner toggle in Settings takes effect immediately. The main screen now shows a "Set up webhook" button instead of an empty panel when no webhook is configured, taking you straight to the webhook section in Settings.
