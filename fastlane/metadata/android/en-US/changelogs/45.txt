Endpoint candidates are now bound to the active Wi-Fi interface: an mDNS service is only registered when its address is currently held by this device's Wi-Fi network, no longer any loopback, link-local or otherwise "known local" address. Without a determinable Wi-Fi address no endpoint is registered at all, and the quick port probe checks further candidates instead of stopping at the first open loopback port (issue #314).
