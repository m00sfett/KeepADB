Keep-Alive now retries on its own after a paused re-enable: first retry after about 2 minutes, then at most every 5 minutes.

The connection view now lists every confirmed way to reach this phone separately - Wi-Fi/LAN, Tailscale/VPN and USB - and marks the main one.

An optional Tailscale line appears only when Tailscale is installed.

Endpoint reports to your own webhook now keep one slot per connection, so they no longer overwrite each other.
