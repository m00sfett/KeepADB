No user-visible change: looked into tightening the check that verifies a wireless-debugging
address found via network discovery (mDNS) so it would confirm the service actually looks like
real adbd, not just that something answers on that port. On-device measurement showed the
stricter check never succeeds against real adbd, so the mDNS check keeps its current, simpler
verification for now while the underlying approach is reconsidered.
