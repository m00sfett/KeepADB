Fixed: the main screen no longer says "waiting for the network" when Wi-Fi is actually connected but blocked by your trusted-network list or an unreadable network — it now tells you which of the two it is.
Added: the main screen now tells you when location permission is missing for trusted Wi-Fi networks and lets you grant it right there — or switch to trusting all Wi-Fi networks instead if you'd rather not.
Added: you're now notified when KeepADB sees a new, untrusted Wi-Fi network even while Wireless Debugging is already running (not just when it's about to turn back on), and when the current network can't be identified at all (e.g. missing location permission), you get a clear notification with a direct link to fix it instead of no notification at all.
